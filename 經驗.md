# 經驗

## 2026-09-18：AI 瀏覽器政策阻擋被誤報為導向逾時

使用者要求：修正 TATWO 在聊天旁開啟 Music OS 時的失敗。
來源：本對話 browser_open 回報 browser_navigation_dispatch_timed_out；browser_read 回報 snapshot_unavailable。
基準：官方 v2.0.9，release target_commitish f53bc8a619c4f658a883c48d4738c8593a0e9c5d。

CEF 的 queueOrDispatchURLString 在 AI 網路政策拒絕時直接返回，不消耗 navigation attempt；Swift 橋接仍等候 attempt，因而逾時。入口應先沿用同一份既有導航政策回報可辨識的拒絕原因，再建立分頁與等候原生派送。原生網路與權限檢查仍須保留，尤其 DNS、重新導向與實際執行期政策不能由前置檢查取代。

本批修正只處理政策阻擋的錯誤回報，不授予 AI 分頁存取本機網路。人用分頁的既有同意流程不因此改變。尚未部署、未宣稱 Music OS 已在聊天旁開啟。

## 2026-09-18：瀏覽器面板不可擠壓左側導覽

使用者截圖顯示並排瀏覽器時左列文字遭裁切，要求開分頁仍保留整個 OS。
來源實作以固定 420/640/960 點配置 inspector，沒有以整個工作區寬度預留側欄與聊天空間。
本批以外層 GeometryReader 取得分割前的可用寬度，將 inspector 上限扣除 250 點側欄、12 點間距、352 點聊天與 1 點分隔。初版小於 935 點時使用 sheet，但 modal 會阻止操作聊天，不符合使用者要求。接續修正改成非模態上下排列，瀏覽器高度不超過 42%／420 點，保留聊天與左列操作。仍待真實 CEF App 視窗縮放與介面驗收，不將幾何測試當成像素驗收。

## 2026-09-18：驗證入口與工作紀錄

外部觀察記錄的是當時狀態，先核對當前 manifest 和原始 log。CoreTests 所需 tatwo-ar-race-helper 必須與測試一起保留；先解析依賴圖，不靠刪測試過關。SwiftPM describe 可能先輸出 resource warnings 再輸出 JSON，保存原 stdout 並分開解析，不把解析器失敗誤當套件依賴失敗。

背景指令直接重導 stdout/stderr 會留下空的 App log。新工作使用 pipefail 與 tee，原生背景卡和證據檔都有輸出；失敗、取消、被取代到成功的關係另存 retry-ledger，不抹掉歷史。測試、Release 產品編譯、簽署打包、安裝、畫面驗收分開記錄。
